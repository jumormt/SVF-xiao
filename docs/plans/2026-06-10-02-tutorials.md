# Plan: svf-harness Tutorials

**Epic:** E1 follow-up (standalone)
**Design:** `docs/designs/2026-06-10-tutorials.md`

## Summary

Six English tutorials (docs/tutorials/01–06) + executable example scripts
(Harness/examples/01–05 + run_all.sh) + ctest wiring. Outputs in docs are real;
scripts assert them; CI keeps them honest.

**Decisions locked in:** structure/decisions per the design doc. Scripts follow
demo/llm_workflow.sh conventions (set -euo pipefail, tempdir socket, trap cleanup,
bounded waits, assertions, "EXAMPLE NN PASSED").

## Phase 1: Examples 01–04 + tutorials 01–04 (fixtures-based)

### [x] Task 1.1: Scripts 01–04 + run_all.sh
- [x] examples/01-getting-started.sh — compile demo.c→.ll, serve, summary, functions, shutdown
- [x] examples/02-exploring.sh — schema excerpts, functions regex, callers/callees (+indirect.c), cfg
- [x] examples/03-pointer-dataflow.sh — pts/aliases/defuse, all three anchor forms, error-hint demo
- [x] examples/04-value-flow.sh — vfpath UAF pattern on demo.c, reachable batch, max_steps elision on chain.c
- [x] run_all.sh runs 01..05, prints per-script PASS/FAIL summary, exit nonzero on any fail
- [x] All scripts pass locally (paste outputs)

run_all.sh summary, 2026-06-10 (failure path also verified with stub scripts → rc=1):

```
==================== summary ====================
01-getting-started.sh        PASS
02-exploring.sh              PASS
03-pointer-dataflow.sh       PASS
04-value-flow.sh             PASS
05-real-world.sh             SKIP (not yet written)
=================================================
RESULT: PASS
```

Notes: side fixtures (indirect.c in 02, chain.c in 04) are queried in
--oneshot mode rather than a second daemon — also documents oneshot in the
tutorials. Watch out: python < 3.12 rejects backslashes in f-strings, so the
inline assertion snippets avoid escaped quotes inside f-strings.

### [x] Task 1.2: Tutorials 01–04 written from real runs
- [x] Each: Goal/Prereqs/Steps(cmd+real output+interpretation)/What you learned/Next
- [x] Outputs pasted from actual script-step runs; trimmed with "..." where long
- [x] Cross-link: each tutorial names its script (README index link lands with
      Task 3.1's docs/tutorials/README.md as planned)

Spot-checked against fresh runs: tutorial 01 summary numbers
(9/93/183/223) and tutorial 03 did-you-mean error message — both identical.
Edge-kind prose in tutorial 04 cross-checked against schema().edge_kinds.

## Phase 2: Real-world + MCP (05–06)

### [ ] Task 2.1: examples/05-real-world.sh + tutorial 05
- [ ] Script: skip-with-message if Test-Suite absent; bc.bc walkthrough (load timing,
      callers free, vfpath malloc→free arg0); bash.bc OPTIONAL stretch behind
      RUN_BIG=1 env (57s load — off by default in CI)
- [ ] Tutorial 05: includes no-debug-info interpretation (loc empty, func+ir usable),
      perf expectations table from shakedown, the xmalloc/sh_xmalloc lesson

### [ ] Task 2.2: Tutorial 06 (MCP) + .mcp.json sample
- [ ] docs/tutorials/06-claude-code-mcp.md: claude mcp add (global + .mcp.json project
      scope), load_program flow, 3 question patterns, sample transcript, troubleshooting
- [ ] examples/mcp-sample.mcp.json with placeholder paths + comment-equivalent README note

## Phase 3: CI wiring + review pass

### [ ] Task 3.1: ctest + docs index + verification
- [ ] CMakeLists: add `harness_examples` test running run_all.sh (BUILD_TESTING-gated;
      env like harness_integration; RUN_BIG unset)
- [ ] `ctest -R harness_examples` passes from Release-build (serial)
- [ ] docs/tutorials/README.md index; Harness/README.md links it
- [ ] Full python suite still green; PROGRESS.md updated

## Verification
- [ ] run_all.sh exits 0 (05 runs when Test-Suite present)
- [ ] ctest -R harness_examples green
- [ ] Every tutorial's pasted outputs match what its script actually produces
- [ ] PROGRESS.md updated with results
